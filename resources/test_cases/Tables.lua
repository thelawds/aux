
x = {
    1, 2, 3,
    [read()] = read(),
    readed = read(),
    read(),
    5, 6, 7,
    {
        table = {"a", "b", "c"},
        "", nil
    }
}

-- TODO Doesn't work: 
-- - Tables inside other tables